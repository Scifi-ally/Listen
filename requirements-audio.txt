# Optional microphone capture and local transcription dependencies.
# The setup script installs these packages and downloads compatible local model weights under ./models.
sounddevice>=0.5,<1
numpy>=1.26,<3
webrtcvad-wheels>=2.0.14,<3
faster-whisper>=1.1,<2
huggingface-hub>=0.25,<1
